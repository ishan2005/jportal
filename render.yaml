services:
  - type: web
    name: jportal
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
